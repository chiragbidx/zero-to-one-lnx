version: "3.9"

services:
  app:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      # Persist installs across restarts for faster boots.
      - node_modules_cache:/app/node_modules
    restart: unless-stopped

volumes:
  node_modules_cache:
